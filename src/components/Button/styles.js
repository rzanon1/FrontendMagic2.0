import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  button: {
    padding: 10,
    backgroundColor: 'black', // Fundo preto
    borderRadius: 5,
    borderWidth: 1, // Adiciona uma borda
    borderColor: '#8B0000', // Cor da borda (vermelha)
  },
  text: {
    color: 'white',
    textAlign: 'center',
  },
});
